# 更新日志

## [0.0.3] - 2026-07-30

### 新增与优化
- 优化分类页 (`categories.php`) 与标签页 (`tags.php`) 显示效果，保持与首页文章列表一致的图片展示效果（默认使用自带的随机图作为缩略图）
- 优化分类与标签页的内置搜索框：修改为纯前端 JavaScript 页面内筛选，避免旧版中直接跳转到全局文章搜索导致用户的疑惑
- 优化侧边栏：侧边栏按钮高亮逻辑使用纯原生 JS 改写，不再受到其他按钮的干扰
- 侧边栏及后台设置：移除旧版的分类按钮 URL 自定义，改为纯开关控制，并结合 Typecho 默认的分类体系独立展示分类和标签
- 为侧边栏按钮新增了默认图标
- 新增 `categories.php` 和 `tags.php` 模板：展示所有分类和标签及其下属文章数量，点击可跳转至对应归档页

### 修复
- **PHP 8.x 兼容性**：修复 `scandir()` 返回 false 时的异常处理（#3）
- **PHP 8.x 兼容性**：修复多处 null 值直接操作导致的 TypeError / Deprecation 警告
- **PHP 8.x 兼容性**：新增 `reEmo()`、`ParseAvatar()`、`GetOs()` 缺失函数定义，与 vCards 原版功能一致
- 修复 `404.php` 中 `header.php`、`sidebar.php`、`footer.php` 路径错误，现已指向 `layout/` 目录
- 修复 `options/index.php` 中未定义变量 `$version` 导致的 PHP 警告
- 修复后台设置页面因为系统原生样式覆盖导致的布局过窄问题

---

## [0.0.2] - 2023

### 介绍
- 基于 vCards 修改的第一个版本
- 简约卡片风格，PJAX 无刷新加载
